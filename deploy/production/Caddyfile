{$CAMPUSPILOT_DOMAIN:localhost} {
    encode zstd gzip

    reverse_proxy agent:8010

    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy strict-origin-when-cross-origin
        -Server
    }

    log {
        output stdout
        format console
    }
}
